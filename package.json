{
  "name": "docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "gen-api-docs": "docusaurus gen-api-docs",
    "clean-api-docs": "docusaurus clean-api-docs",
    "docusaurus": "docusaurus",
    "start": "yarn clean-api-docs all && yarn gen-api-docs all && docusaurus start",
    "build": "yarn clean-api-docs all && yarn gen-api-docs all && docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "yarn clean-api-docs all && yarn gen-api-docs all && docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": ">=2.0.1 <2.3.0",
    "@docusaurus/preset-classic": ">=2.0.1 <2.3.0",
    "@mdx-js/react": "^1.6.22",
    "clsx": "^1.2.1",
    "docusaurus-plugin-openapi-docs": "^1.7.3",
    "docusaurus-theme-openapi-docs": "^1.7.3",
    "prism-react-renderer": "^1.3.5",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "2.0.1"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=16.14"
  }
}
